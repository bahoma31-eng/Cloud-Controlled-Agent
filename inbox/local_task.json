{
  "task_id": "gmaps_112",
  "engine": "POWERSHELL",
  "command": "$script = @'\nimport json, urllib.request, struct, base64, os, socket, time\n\ndef cdp_get_pages():\n    r = urllib.request.urlopen(\"http://localhost:9222/json\", timeout=5)\n    return json.loads(r.read())\n\ndef ws_cmd(ws_url, msg, wait=3):\n    from urllib.parse import urlparse\n    u = urlparse(ws_url)\n    host = u.hostname\n    port = u.port or 80\n    s = socket.create_connection((host, port), timeout=15)\n    key = base64.b64encode(os.urandom(16)).decode()\n    req = f\"GET {u.path} HTTP/1.1\\r\\nHost: {host}:{port}\\r\\nUpgrade: websocket\\r\\nConnection: Upgrade\\r\\nSec-WebSocket-Key: {key}\\r\\nSec-WebSocket-Version: 13\\r\\n\\r\\n\"\n    s.sendall(req.encode())\n    resp = b\"\"\n    while b\"\\r\\n\\r\\n\" not in resp:\n        resp += s.recv(4096)\n    data = msg.encode(\"utf-8\")\n    frame = bytearray([0x81])\n    l = len(data)\n    mask_key = os.urandom(4)\n    if l < 126:\n        frame.append(0x80 | l)\n    elif l < 65536:\n        frame.append(0x80 | 126)\n        frame.extend(struct.pack(\">H\", l))\n    frame.extend(mask_key)\n    masked = bytearray(b ^ mask_key[i%4] for i,b in enumerate(data))\n    frame.extend(masked)\n    s.sendall(frame)\n    time.sleep(wait)\n    s.settimeout(5)\n    all_data = b\"\"\n    try:\n        while True:\n            chunk = s.recv(65536)\n            if not chunk: break\n            all_data += chunk\n            if len(chunk) < 65536: break\n    except: pass\n    s.close()\n    result = \"\"\n    pos = 0\n    while pos < len(all_data):\n        if pos+2 > len(all_data): break\n        b1 = all_data[pos+1] & 0x7F\n        hlen = 2\n        if b1 == 126: hlen = 4\n        elif b1 == 127: hlen = 10\n        if pos+hlen > len(all_data): break\n        if b1 == 126:\n            plen = struct.unpack(\">H\", all_data[pos+2:pos+4])[0]\n        elif b1 == 127:\n            plen = struct.unpack(\">Q\", all_data[pos+2:pos+10])[0]\n        else:\n            plen = b1\n        payload = all_data[pos+hlen:pos+hlen+plen]\n        result += payload.decode(\"utf-8\", errors=\"replace\")\n        pos = pos+hlen+plen\n    return result\n\npages = cdp_get_pages()\ntarget = None\nfor p in pages:\n    if \"maps\" in p[\"url\"] and \"contrib\" in p[\"url\"] and \"_/js/\" not in p[\"url\"]:\n        target = p\n        break\nif not target:\n    target = pages[0]\nws = target[\"webSocketDebuggerUrl\"]\nprint(\"Navigating to add-a-place...\")\nnav = json.dumps({\"id\":1,\"method\":\"Page.navigate\",\"params\":{\"url\":\"https://www.google.com/maps/contrib/0/add-a-place\"}})\nr = ws_cmd(ws, nav, wait=8)\nprint(\"Nav done. Inspecting...\")\njs = 'JSON.stringify({t:document.title,u:location.href,inp:Array.from(document.querySelectorAll(\"input\")).map(i=>({n:i.name,a:i.getAttribute(\"aria-label\"),tp:i.type,ph:i.placeholder,v:i.offsetParent!==null})).filter(x=>x.v).slice(0,20),btn:Array.from(document.querySelectorAll(\"button,a[role=button]\")).map(b=>b.innerText.trim()).filter(t=>t&&t.length<60).slice(0,15)})'\ncmd = json.dumps({\"id\":2,\"method\":\"Runtime.evaluate\",\"params\":{\"expression\":js}})\nr2 = ws_cmd(ws, cmd, wait=2)\nprint(f\"Result: {r2[:1000]}\")\n'@\n$script | Out-File -FilePath 'C:\\temp\\cdp_addplace.py' -Encoding UTF8 -Force\npython C:\\temp\\cdp_addplace.py",
  "description": "CDP - الانتقال لصفحة add-a-place عبر Maps وفحص النموذج",
  "timeout": 30
}