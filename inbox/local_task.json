{
  "task_id": "TEST-003b",
  "engine": "POWERSHELL",
  "command": "python -c \"import sys; sys.path.insert(0, r'C:\\Users\\Revexn\\CodeAnalyzer'); from code_analyzer import CodeAnalyzer; analyzer = CodeAnalyzer(r'C:\\Users\\Revexn\\CodeAnalyzer'); print('SUCCESS: CodeAnalyzer instance created'); print('Target path:', analyzer.target_path); methods = [m for m in dir(analyzer) if not m.startswith('_') and callable(getattr(analyzer, m))]; print('Public methods:', len(methods)); print('Methods:', ', '.join(methods[:20]))\"",
  "description": "اختبار 3b: إنشاء كائن CodeAnalyzer وفحص الطرق المتاحة (تصحيح اسم السمة)",
  "timeout": 60
}